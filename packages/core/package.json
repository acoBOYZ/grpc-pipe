{
  "name": "@grpc-pipe/core",
  "version": "0.1.56",
  "author": "ACO .",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "keywords": [
    "grpc",
    "grpc communication",
    "grpc streaming",
    "pipe messaging",
    "typed communication",
    "type-safe rpc",
    "nodejs grpc",
    "bun grpc",
    "typescript grpc",
    "fast messaging",
    "binary protocol",
    "real-time messaging",
    "streaming rpc",
    "full duplex grpc",
    "custom transport protocol",
    "schema-based messaging",
    "protobuf transport",
    "grpc bidirectional stream",
    "typescript ipc",
    "efficient rpc"
  ],
  "engines": {
    "bun": ">=1",
    "node": ">=18"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js"
    }
  },
  "sideEffects": false,
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "clean": "rimraf ./dist && rimraf ./node_modules",
    "build": "bun run proto:compile && tsc -p tsconfig.json && echo '[@grpc-pipe/core] build done ✅'",
    "proto:compile": "protoc --ts_proto_out=src/types --ts_proto_opt=outputServices=grpc-js --proto_path=proto proto/*.proto"
  },
  "devDependencies": {}
}
