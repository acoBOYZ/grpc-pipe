{
  "name": "@grpc-pipe/server",
  "version": "0.1.51",
  "author": "ACO .",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "keywords": [
    "grpc",
    "grpc communication",
    "grpc streaming",
    "pipe messaging",
    "typed communication",
    "type-safe rpc",
    "nodejs grpc",
    "bun grpc",
    "typescript grpc",
    "fast messaging",
    "binary protocol",
    "real-time messaging",
    "streaming rpc",
    "full duplex grpc",
    "custom transport protocol",
    "schema-based messaging",
    "protobuf transport",
    "grpc bidirectional stream",
    "typescript ipc",
    "efficient rpc"
  ],
  "engines": {
    "bun": ">=1",
    "node": ">=18"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js"
    }
  },
  "sideEffects": false,
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "clean": "rimraf ./dist && rimraf ./node_modules && rimraf ./coverage",
    "build": "bun build ./src/index.ts --outdir dist --target node && bun run type:build && echo '[@grpc-pipe/server] build done ✅'",
    "type:build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@grpc-pipe/core": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.2.9",
    "@types/node": "^22.14.1",
    "typescript": "^5.8.3"
  }
}
